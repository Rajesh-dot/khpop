import { ref, onMounted, watch } from 'vue';
//import { Plugin, CameraResultType, CameraSource, CameraPhoto, Capacitor, FileSystemDirectory } from '@capacitor/core';

export default function usePhotoGallery(){}